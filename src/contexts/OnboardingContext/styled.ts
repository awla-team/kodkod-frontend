import styled from 'styled-components';
import { Fab } from '@mui/material';

export const TourFab = styled(Fab)`
  position: absolute;
  bottom: 16px;
  height: 60px;
  width: 60px;
  right: 90px;
`;
